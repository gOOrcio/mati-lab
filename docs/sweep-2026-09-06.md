# Full-stack sweep — 2026-09-06

Full health sweep of every deployed surface (Network Pi, TrueNAS, Proxmox +
VMs/LXCs, Homebridge Pi, dev PC) with fixes applied same-day. This file is
the record of what was found, what was fixed, and what remains.

## Fixed during the sweep

| # | Issue | Root cause | Fix |
|---|---|---|---|
| 1 | **Alerting silently dead ~3 months** — Alertmanager→ntfy 403 on every notify since ~2026-06-07 (52k failed attempts; `ScrapeTargetDown` firing since 06-19 never delivered) | ntfy runs `auth-default-access: deny`; the webhook had no credentials and no ntfy user existed for it (lost in the June Pi reinstall era) | New ntfy user `alertmanager` (write-only ACL on `homelab-alerts`), token at `network/alertmanager/ntfy-token` on the Pi (0600, owner 65534, gitignored), `http_config.authorization.credentials_file` wired in `alertmanager.yml`. Verified "Notify success" in logs. Rotation row in `nas/secrets-inventory.md`. |
| 2 | **Dev PC restic backups silently blocked 3.5 weeks** (no snapshots 08-13 → 09-06) | Interrupted prune left an exclusive lock held by a dead PID; every nightly run failed on it | `restic unlock`, verified full run + Sunday prune; `restic unlock` added at script start (dead-PID locks only) |
| 3 | **Pi-hole gravity DB corruption errors** (SQLite "database disk image is malformed" at 05:17) | Corrupt gravity.db image; nebula-sync's hourly rebuild self-healed it at 09:17 | `PRAGMA integrity_check` → ok on gravity + FTL DBs; fresh `pihole -g` rebuild; DNS + blocking verified (506k domains) |
| 4 | **fail2ban dead on the Pi since June** | Ubuntu noble ships fail2ban 1.0.2, incompatible with Python 3.12 (`asynchat` removed); no fixed package in noble | Installed Debian's `fail2ban_1.1.0-11_all.deb` (pure-Python), `apt-mark hold`, sshd jail active on journald backend |
| 5 | **fail2ban + 5 other failed units on both LXCs** (110 smart-resume, 111 restorate); SSH logins stalled 25 s | Unprivileged LXC without `nesting=1` → systemd NAMESPACE sandboxing fails; separately, Debian 13 has no auth.log for the file-backend sshd jail | `pct set {110,111} -features nesting=1` + reboot; `systemd-networkd` masked (ifupdown is the real network config); fail2ban switched to `backend = systemd`. Both LXCs: 0 failed units, logind OK, apps healthy |
| 6 | **Proxmox daily `apt-get update` failing** | Enterprise repos enabled with no subscription; no no-subscription repo | Enterprise pve + ceph `.sources` disabled (backups in `/root/*.bak-2026-09-06`), `proxmox.sources` (pve-no-subscription, trixie) added; `pveupdate` exits 0 |
| 7 | **Ollama VM root disk 100% full** (452 MB free) | Stale duplicate model store `/var/lib/ollama` (15G, Feb-era gemma2/llama3.1) alongside the live `/usr/share/ollama/.ollama/models` (29G) | Verified live store via manifests + `/api/tags`, removed the stale store, apt/journal cleanup → **73% used, 16G free**; Ollama healthy |
| 8 | **Pi checkout 4 commits behind** | Renovate merges not deployed | Deployed ntfy v2.27, Loki 3.7.6, cAdvisor v0.55.1; all healthy |
| 9 | **`vpn-port-mismatch` Kuma monitor red for ~86 days** (probe: "gluetun forwarded_port empty", 4126 consecutive) | PF actually works (qBit holds granted port 58308). gluetun v3.41 truncates `/tmp/gluetun/forwarded_port` on internal VPN health cycles and only rewrites on port *change* — empty file ≠ dead lease | Probe fallback added: empty file → last "port forwarded is N" line from gluetun logs. Probe now pushes `OK gluetun=qbit=58308`. Durable fix deferred (see below) |
| 10 | **arr-config-backup bazarr cp bug** (`bazarr_temp.db\nbazarr_temp.db-journal` mangled into one path) | Multi-file dir diff when Bazarr drops transient temp files | Diff now filters to `*.zip` + `tail -1`. Installed on NAS |
| 11 | **Nightly Kuma push heartbeat failures from NAS** (curl 7/28 at 03:15–04:15 CEST) | Transient nightly window (coincides with Pi-hole gravity/nebula-sync activity); Kuma has no host port so a LAN-IP bypass is impossible | `--retry 3 --retry-delay 45 --retry-all-errors` added to every heartbeat curl in all 7 backup scripts |
| 12 | **CouchDB admin credential embedded in cron command** (visible in `cronjob.query`, ps, middleware DB) | Original Phase 5 install shortcut | Password **rotated** (API + app compose env, redeploy verified), cred moved to `/root/.backup-env`, cron now runs committed `nas/backup-jobs/obsidian-couchdb-dump.sh`; dump verified twice. **Re-rotate at your leisure** — the old value transited this session (procedure in `nas/secrets-inventory.md`) |
| 13 | **Repo/live drift: `hermes-backup.sh`** — repo version pointed at the pre-2026-07-31 `ix-hermes-hermes-*` container + dead data path | July live-only hotfix never committed | Live version recovered from ZFS snapshot, merged into repo (current `ix-hermes-agent-*` + `/mnt/fast/databases/hermes/data`); fresh 130 MB artefact verified |

## Needs you (action items)

1. **Homebridge backups dead since Jun 21** — the UI admin credentials in
   `/root/.backup-env` on the NAS get 403 from `POST /api/auth/login`
   (the UI password evidently changed without the env being updated).
   - Try logging into `http://192.168.1.155:8581` with PM
     `homelab/homebridge/admin`. If that works, only the NAS env is stale;
     if not, reset the password via the UI first.
   - Then stage the working password (no heredoc; single command):

   ```bash
   read -rs PW && ssh truenas_admin@192.168.1.65 "sudo sed -i '/^HOMEBRIDGE_PASSWORD=/d' /root/.backup-env && sudo tee -a /root/.backup-env <<<\"HOMEBRIDGE_PASSWORD=$PW\" >/dev/null" && unset PW
   ```

   - Verify: `ssh truenas_admin@192.168.1.65 'midclt call cronjob.run 21'`,
     then check a fresh file in `bulk/backups/homebridge/` and the
     `backup-homebridge-dump` Kuma monitor.
2. **Obsidian vault stale on NAS since 2026-05-01** — Syncthing on the NAS is
   healthy, so the Mac side has most likely been signed out / stopped since
   May. Open Syncthing on the Mac and check the folder is sharing; the
   vault-rag index is serving 4-month-old content until this syncs.
3. **Re-rotate two secrets that transited this session** (both work now;
   this is hygiene): CouchDB admin (procedure in `nas/secrets-inventory.md`
   Obsidian row) and optionally the ntfy `alertmanager` token (ntfy row).
   Save both in PM (`homelab/obsidian/couchdb-admin`, `homelab/ntfy/alertmanager`).
4. **Confirm decommissions**: `sentinel-trader` (192.168.1.202) and
   `sonarqube` (192.168.1.201) no longer exist on Proxmox at all. If
   intentional, say so and I'll clean up: Prometheus target for `.202`
   (feeding the now-live `ScrapeTargetDown` alerts), the host-table rows in
   `~/.claude/CLAUDE.md`, and the secrets-inventory Sentinel rows.
   Same question for stopped **VM 100** (duplicate `ollama-gpu`) which still
   burns a ~2.8 GB weekly vzdump slot.
5. **`restorate-dev` scrape target** (192.168.1.173 / mati-gamer) is down —
   presumably that box is just off; it now generates real alert
   notifications. Tell me if it should leave `prometheus.yml`.

## Deferred (complex, written up — added to followups.md)

- **Pi containerd image store wears the SD card** (12G in `/var/lib/containerd`
  on mmcblk0; the NVMe relocation only moved `/var/lib/docker`). Fix =
  migrate containerd root to NVMe (service downtime for every container).
- **gluetun PF durable observability** — add a control-server auth config
  (`config.toml` role for `GET /v1/openvpn/portforwarded`) or bump gluetun
  and point the probe at the API instead of the truncation-prone file.
- **`bulk/backups/network-pi` dataset shadowing** — the Pi's nightly service
  backups (02:00, working fine) land in the *parent* dataset's directory,
  hidden under an empty child dataset mounted over it. NAS-side `ls` shows
  nothing; the data is there and snapshotted via the parent. Fix = quiesce
  NFS briefly, `pool.dataset.delete bulk/backups/network-pi` (EBUSY while
  NFS pins it).
- **Promtail "timestamp too old" 400 noise** (~300/day) — stale positions
  for containers that stopped logging. Deliberately not fixed: clearing
  positions would re-ingest retained logs as duplicates. Noise is bounded.

## Accepted / no action

- **LiteLLM 30-min probe noise** (`/props`, `/version` 404 → `/v1/models/agent-default`
  401 → `/v1/models` 200, source = docker gw IP, always-on client, active
  before the dev PC booted): a provider-autodetect poll, almost certainly
  Hermes. The 401 is correct RBAC (model-info route is admin-only for
  virtual keys). Harmless.
- **Pi-hole version drift** (Core 6.3 vs 6.4.3): pinned image, Renovate owns bumps.
- **UDR/mati-gamer ping blips, cloudflared QUIC reconnect, diun Docker Hub 500**: transient, self-recovered.
- **smart-resume `/health` 404 on the API port** (Caddy :80 fronts fine) — no
  localhost health route exists; Kuma monitors via Caddy already.

## Healthy confirmations (sweep evidence)

- ZFS: all pools healthy, scrubs clean (08-30 / 09-04), capacity 29–33%.
- All 21 TrueNAS apps RUNNING; all 30 NAS containers + all 22 Pi containers up, none restarting.
- Backups fresh today: gitea/litellm pg_dumps, hermes zip, arr weekly, obsidian CouchDB dump, Proxmox vzdump (4 guests, all OK, 09-06 01:00–01:13), dev-PC restic (post-fix), ZFS-health cron.
- Qdrant `obsidian-vault` green (180 pts, 768d cosine); vault-rag MCP answers end-to-end; LiteLLM/Gitea/Grafana/Loki/Prometheus/ntfy/Authelia/Caddy all healthy; TLS valid to Nov 5 (auto-renew).
- Gitea runner: 3 months without a failed job; hourly cache prune active.
