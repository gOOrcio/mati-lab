# Token of the dedicated "mapa" tunnel (Zero Trust -> Networks -> Tunnels -> mapa).
# NOT the main homelab tunnel token. Value lives in the password manager.
MAPA_TUNNEL_TOKEN=<from password manager>
