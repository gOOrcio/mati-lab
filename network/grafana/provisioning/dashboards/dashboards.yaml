# Dashboard provisioning disabled — dashboards live in Grafana's DB.
# Edit dashboards freely in the UI. Nightly export backs them up to git + NAS.
# To push a dashboard programmatically, use the Grafana HTTP API:
#   POST /api/dashboards/db  (see manage-grafana.sh import)
apiVersion: 1
providers: []
