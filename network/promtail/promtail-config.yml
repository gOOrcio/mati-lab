server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /var/lib/promtail/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: tcp://docker-socket-proxy:2375
        refresh_interval: 10s
    relabel_configs:
      # Use container name as the primary label
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      # Add compose project label
      - source_labels: ['__meta_docker_container_label_com_docker_compose_project']
        target_label: 'compose_project'
      # Add compose service label
      - source_labels: ['__meta_docker_container_label_com_docker_compose_service']
        target_label: 'compose_service'
      # Add host label
      - target_label: 'host'
        replacement: 'network-pi'
      # Drop promtail and socket proxy logs to avoid recursion
      - source_labels: ['__meta_docker_container_name']
        regex: '/(promtail|docker-socket-proxy)'
        action: drop

  # ── UniFi UDR syslog ──────────────────────────────────────────────────────
  # Added 2026-09-10. The UDR's own log view is short-lived and, critically,
  # not queryable: stat/event, stat/alarm and stat/ips/event all reject
  # Integration API keys, so firewall denials could only be read by hand in the
  # UI. Shipping them here makes them searchable, correlatable with Pi/NAS logs,
  # retained 30 days, and alertable.
  #
  # UniFi sends RFC3164 (BSD), not RFC5424 — promtail defaults to the latter and
  # silently drops what it cannot parse, so syslog_format is mandatory here.
  # Security events arrive as CEF inside the message body.
  - job_name: unifi-syslog
    syslog:
      listen_address: 0.0.0.0:1514
      listen_protocol: udp
      syslog_format: rfc3164
      idle_timeout: 60s
      label_structured_data: false
      labels:
        job: unifi
        host: udr
    relabel_configs:
      - source_labels: ['__syslog_message_hostname']
        target_label: 'unifi_host'
      - source_labels: ['__syslog_message_severity']
        target_label: 'severity'
    pipeline_stages:
      # Firewall / IPS events arrive as CEF. Extract the few fields worth
      # querying on. NOTE: Go uses RE2 — no lookahead/lookbehind. The client
      # alias may contain spaces, so match non-greedily up to the next CEF key.
      - match:
          selector: '{job="unifi"} |= "CEF:"'
          stages:
            - regex:
                expression: 'UNIFIpolicyName=(?P<policy>\S+)'
            - regex:
                expression: '\bact=(?P<action>\S+)'
            - regex:
                expression: '\bapp=(?P<app>\S+)'
            - regex:
                expression: 'UNIFIsrcClientAlias=(?P<src_client>.*?) UNIFI'
            - regex:
                expression: '\bdst=(?P<dst>\S+)'
            - regex:
                expression: '\bdpt=(?P<dport>\d+)'
            # Only low-cardinality fields become labels. dst / dport /
            # src_client stay as extracted fields for line filtering — a label
            # per destination IP would blow up the index.
            - labels:
                policy:
                action:
                app:
